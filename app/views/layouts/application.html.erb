<!DOCTYPE html>
<html>
  <head>
    <title>TrendyCo</title>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.2/jquery.min.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

  </head>

  <body>
  <header id="top_header" class="clearfix">
    <div class="wrapper">
      <a href="/static_pages/landing_page"><h1 class="logo">Trendy<span>&amp;</span>Co</h1></a>
      <a href="#" class="menu"><i class="fa fa-bars"></i></a>
      <nav id="main_nav">
          <%= link_to "Shop", products_path %>
          <%= link_to "About", static_pages_about_path %>
          <%= link_to "Contact", new_contact_path %>
          <%= link_to "Blog", posts_path %>
          <%= form_tag("/products", method: "get", id: "search-form") do %>
            <%= text_field_tag :search, params[:q], placeholder: "Search" %>
          <% end %>
          <% if user_signed_in? %>
            <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
          <% else %>
            <%= link_to('Login', new_user_session_path) %>
          <% end %>
      </nav>
    </div>
  </header>

    <div class="container">
      <% if notice %>
          <p id="notice" class="alert alert-info"><%= notice %></p>
      <% end %>
       <% if notice %>
          <p id="alert" class="alert alert-info"><%= alert %></p>
       <% end %>
    </div>

    <%= yield %>

    <footer id="main_footer">
      <p class="logo">Trendy<span>&</span>Co</p>
      <p class="copyright">@2017 TrendyCo. All Rights Reserved.</p>
      <div class="links">
        <a href="#">Terms of Service</a>
        <a href="#">Private Policy</a>
      </div>
     </footer>

  </body>
</html>
